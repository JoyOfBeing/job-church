// Feature flags — flip these to true on launch day
// These gate visibility of paid features

export const FLAGS = {
  MEMBERSHIP_ENABLED: false,
  DEPROGRAMMING_ENABLED: false,
  ELDERS_ENABLED: false,
};
